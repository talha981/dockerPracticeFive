version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80" # Host port 3000 maps to container port 80
    restart: always
    container_name: react-container
